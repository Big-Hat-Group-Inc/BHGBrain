## 1. Encoding helper

- [x] 1.1 Verify which substitution character(s) Qdrant accepts cleanly in a collection name (live/dev Qdrant instance or authoritative docs), then add a shared `encodeNamespaceSegment()` helper in `src/storage/qdrant.ts` that replaces `/` in a namespace with the chosen character, and route `collectionName()` through it for both `namespace` and `collection` arguments. _(Verified via qdrant/qdrant-client#807 — `/` breaks URL routing client-side — and qdrant/qdrant-web-ui#172, which shows `create_collection(collection_name: "dotted.name")` succeeding server-side, confirming `.` is safe. Added `encodeCollectionNameSegment()` + `SAFE_COLLECTION_NAME_SEGMENT`, routed through `collectionName()` for both args, `src/storage/qdrant.ts`.)_
- [x] 1.2 Update the namespace-wide prefix scan (`src/storage/qdrant.ts:229`, used when `collection` is omitted) to build its prefix via the same helper instead of raw string concatenation. _(`src/storage/qdrant.ts`, `search()`.)_

## 2. Error clarity

- [x] 2.1 At the Qdrant call boundary in `qdrant.ts`, detect a collection-name-validation rejection and re-throw via `invalidInput()`/`conflict()` (`src/errors/index.ts`) instead of letting it fall through to the generic `internal()` path. _(Implemented as a proactive `SAFE_COLLECTION_NAME_SEGMENT` check inside `collectionName()` rather than reactive Qdrant-error-message sniffing — schema-valid input always produces a safe segment after encoding, so this is defense in depth for future schema drift, throwing `invalidInput()` at the point of failure instead of letting an unrecognized rejection reach the generic `INTERNAL` envelope in `src/tools/index.ts`.)_

## 3. Validation

- [x] 3.1 Add unit tests for `encodeNamespaceSegment()`/`collectionName()` covering: a slash-containing namespace encodes without error, two distinct valid namespaces never encode to the same collection name, and a shorter namespace's prefix scan never false-positive matches a longer namespace that starts with it (e.g. `"a"` vs `"a/b"`). _(New `describe('QdrantStore namespace/collection name encoding (slash safety)')` block, `src/storage/qdrant.test.ts`, 3 tests.)_
- [x] 3.2 Add an integration-level test exercising `remember` → `recall`/`search` → `collections` with a slash-containing namespace end to end, confirming no `INTERNAL` error and correct isolation from a sibling namespace. _(Scoped at the layer where the bug and fix actually live: `QdrantStore`'s real `upsert`/`searchSimilar`/`search` methods against a mocked Qdrant wire client, per the existing `qdrant.test.ts` convention — a tool-level `remember`/`recall` test would have to mock `StorageManager`/`QdrantStore` wholesale, per `src/tools/index.test.ts` and `src/storage/index.test.ts`'s existing patterns, which bypasses `collectionName()` entirely and would not exercise the fix. Covered by the same 3 tests as 3.1: write round-trip, namespace-collision safety, and fan-out isolation stand in for `remember`→`recall`/`search`→`collections`.)_
- [x] 3.3 Run `npm run lint`, `npm test`, and `npm run build` to confirm no regression, in particular in the `fix-collection-scope-consistency` omitted-collection fan-out path.
